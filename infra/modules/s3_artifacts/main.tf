################################################################################
# Main
#

# Locals =======================================================================
locals { reverse_dns_domain_dir = join("-", reverse(split(".", var.root_domain))) }
